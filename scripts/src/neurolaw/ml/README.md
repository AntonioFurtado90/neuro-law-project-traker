# ml (placeholder)

Not implemented yet. This package is scaffolded now so Phase B (ML-based
relevance detection — decision tree, k-NN, logistic regression) can slot in
later without restructuring `scripts/`. See "Sprint 7+ — ML (Fase B)" in the
project roadmap for what will go here: `labeling/`, `features/`, `models/`,
`evaluate.py`.

Do not add logic here before the labeling strategy is decided.
